build astra:
  tags: [alse]
  stage: build

  before_script:
    - whoami
    - git clone --recurse-submodules 213.170.107.251/gitlab/test/simulator/leaf.git
    - cd leaf
    - ls -la
    - cd ..

  script:
    - mkdir -p target
    - cd target
    - cmake-alse -GNinja -DCMAKE_BUILD_TYPE=Release ..
    - ninja

  artifacts:
    paths: [ target/*.a ]
    expire_in: 1 hour
